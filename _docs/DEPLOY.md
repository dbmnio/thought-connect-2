npx expo install react-native-skia

Install ndk via android studio

ANDROID_NDK=/Users/username/Library/Android/sdk/ndk-bundle
(for skia)

npx expo install react-native-gesture-handler

npx expo prebuild (for gestures)